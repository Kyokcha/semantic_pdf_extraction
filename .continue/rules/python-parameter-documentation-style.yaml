name: Python Parameter Documentation Style
version: 0.0.1
schema: v1
rules:
  - name: Python Parameter Documentation Style
    rule: |-
      Document parameters in Args section using the format:
          param_name (type): Description
      Not:
          param_name: type description
    globs: "**/*.py"
